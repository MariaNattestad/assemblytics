#!/bin/bash

# Author: Maria Nattestad
# github.com/marianattestad/assemblytics
#
# Thin launcher for the Python orchestrator. Kept for backward compatibility
# with the web app and existing command-line usage.

SCRIPT_PATH=$(dirname "$BASH_SOURCE")
exec python3 "$SCRIPT_PATH/Assemblytics.py" "$@"
